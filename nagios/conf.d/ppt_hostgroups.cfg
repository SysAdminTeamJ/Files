# HEADER: This file was autogenerated at 2018-05-05 16:27:43 +1200
# HEADER: by puppet.  While it can still be managed manually, it
# HEADER: is definitely not recommended.
define hostgroup {
	alias                          Database Servers
	hostgroup_name                 db-servers
	members                        db.foo.org.nz
}

define hostgroup {
	alias                          Remote Disks NRPE
	hostgroup_name                 remote-disks
	members                        db.foo.org.nz, storage.foo.org.nz, app.foo.org.nz
}

define hostgroup {
	alias                          Windows Servers
	hostgroup_name                 windows-servers
	members                        ad.directory.foo.org.nz
}

define hostgroup {
	alias                          HTTP Servers
	hostgroup_name                 http-servers
	members                        localhost
}

define hostgroup {
	alias                          SSH Servers
	hostgroup_name                 ssh-servers
	members                        localhost
}

define hostgroup {
	alias                          Ubuntu Linux Servers
	hostgroup_name                 ubuntu-linux
	members                        localhost, db.foo.org.nz, storage.foo.org.nz, app.foo.org.nz
}
